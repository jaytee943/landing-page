---
interface Props {
  label: string;
  class?: string;
}

const { label, class: className } = Astro.props;
---

<div class={className}>
  <div class="image-placeholder" role="img" aria-label={label}>{label}</div>
</div>
